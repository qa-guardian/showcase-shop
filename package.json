{
  "name": "acme-orders",
  "private": true,
  "version": "1.0.0",
  "description": "Acme Orders — a tiny shop tested by QA Guardian (static, login-free storefront).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host --port 4173"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^8.3.0"
  }
}
